# Copy to .env and adjust. Never commit .env (see root .gitignore).

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=system_rekomendacji

# Optional: only if you add a TMDB sync script (not used by uvicorn at runtime today)
# TMDB_API_KEY=
